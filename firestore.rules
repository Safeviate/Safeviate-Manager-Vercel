rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Universal recursive match for development: Allow all operations for authenticated users.
    // This ensures Barry and others have seamless access to all tenant data and sub-collections.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}