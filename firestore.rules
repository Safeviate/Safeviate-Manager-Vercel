rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Definitive recursive match: Allow all operations for authenticated users.
    // This ensures Barry and all other personnel have seamless access to tenant data and bookings.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
