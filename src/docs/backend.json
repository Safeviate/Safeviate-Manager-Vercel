
{
  "entities": {
    "PublishedTable": {
      "title": "Published Table",
      "type": "object",
      "properties": {
        "pageId": { "type": "string" },
        "tableData": { "$ref": "#/backend/entities/TableTemplate/properties/tableData" }
      },
      "required": ["pageId", "tableData"]
    },
    "TableTemplate": {
        "title": "Table Template",
        "type": "object",
        "properties": {
            "name": { "type": "string" },
            "tableData": {
                "type": "object",
                "properties": {
                    "rows": { "type": "number" },
                    "cols": { "type": "number" },
                    "cells": { 
                        "type": "array",
                        "items": {
                            "type": "object",
                            "properties": {
                                "r": { "type": "number" },
                                "c": { "type": "number" },
                                "content": { "type": "string" },
                                "rowSpan": { "type": "number" },
                                "colSpan": { "type": "number" },
                                "hidden": { "type": "boolean" }
                            },
                            "required": ["r", "c", "rowSpan", "colSpan", "hidden"]
                        }
                    },
                    "colWidths": { "type": "array", "items": { "type": "number" } },
                    "rowHeights": { "type": "array", "items": { "type": "number" } }
                },
                "required": ["rows", "cols", "cells", "colWidths", "rowHeights"]
            }
        },
        "required": ["name", "tableData"]
    },
    "SummarizeDocumentOutput": {
        "title": "Summarize Document Output",
        "type": "object",
        "properties": {
            "requirements": {
                "type": "array",
                "items": {
                    "$ref": "#/backend/entities/Regulation"
                }
            }
        }
    },
    "Regulation": {
        "title": "Regulation",
        "type": "object",
        "properties": {
            "regulationCode": { "type": "string" },
            "regulationStatement": { "type": "string" },
            "technicalStandard": { "type": "string" },
            "companyReference": { "type": "string" },
            "parentRegulationCode": { "type": "string", "description": "The code of the parent regulation, if this is a sub-regulation." }
        }
    },
    "FindingLevel": {
        "title": "Finding Level",
        "type": "object",
        "description": "Represents a single audit finding level with its associated color.",
        "properties": {
            "id": { "type": "string" },
            "name": { "type": "string" },
            "color": { "type": "string", "description": "The hex color code for the background of this level." },
            "foregroundColor": { "type": "string", "description": "The hex color code for the text of this level." }
        },
        "required": ["id", "name", "color", "foregroundColor"]
    },
    "FindingLevelsSettings": {
        "title": "Finding Levels Settings",
        "type": "object",
        "description": "Represents the audit finding levels settings for a tenant.",
        "properties": {
            "id": { "type": "string" },
            "levels": {
                "type": "array",
                "items": { "$ref": "#/backend/entities/FindingLevel" }
            }
        },
        "required": ["id", "levels"]
    },
    "ComplianceRequirement": {
        "title": "Compliance Requirement",
        "type": "object",
        "description": "A single regulatory or internal compliance requirement.",
        "properties": {
            "id": { "type": "string" },
            "regulationCode": { "type": "string", "description": "The official code for the regulation (e.g., '141.02.2')." },
            "parentRegulationCode": { "type": "string", "description": "The code of the parent regulation, if this is a sub-regulation." },
            "regulationStatement": { "type": "string", "description": "The short, official title or heading of the regulation." },
            "technicalStandard": { "type": "string", "description": "The full, detailed text body of the regulation." },
            "companyReference": { "type": "string", "description": "A reference to the internal manual or procedure (e.g., 'Ops Manual, Sec 4.2.1')." },
            "responsibleManagerId": { "type": "string", "description": "The ID of the manager responsible for this requirement." },
            "lastAuditDate": { "type": "string", "format": "date-time" },
            "nextAuditDate": { "type": "string", "format": "date-time" }
        },
        "required": ["id", "regulationCode", "regulationStatement", "companyReference"]
    },
    "FlightPlan": {
      "title": "Flight Plan",
      "type": "object",
      "description": "Represents a comprehensive flight plan including route, W&B, and performance calculations.",
      "properties": {
        "id": { "type": "string" },
        "bookingId": { "type": "string", "description": "The booking this flight plan is associated with." },
        "pilotId": { "type": "string" },
        "aircraftId": { "type": "string" },
        "departure": { "type": "string" },
        "destination": { "type": "string" },
        "alternate": { "type": "string" },
        "flightRules": { "type": "string", "enum": ["VFR", "IFR"] },
        "cruisingAltitude": { "type": "number" },
        "waypoints": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "name": { "type": "string" },
              "type": { "type": "string", "enum": ["Airport", "VOR", "NDB", "GPS"] },
              "lat": { "type": "number" },
              "lon": { "type": "number" }
            }
          }
        },
        "massAndBalance": { "$ref": "#/backend/entities/MassAndBalance" },
        "performance": {
          "type": "object",
          "properties": {
            "cruiseSpeed": { "type": "number" },
            "fuelBurnRate": { "type": "number" },
            "windDirection": { "type": "number" },
            "windSpeed": { "type": "number" }
          }
        },
        "navLog": {
          "type": "array",
          "description": "Calculated navigation log for each leg of the journey.",
          "items": {
            "type": "object",
            "properties": {
              "from": { "type": "string" },
              "to": { "type": "string" },
              "track": { "type": "number" },
              "windCorrectionAngle": { "type": "number" },
              "heading": { "type": "number" },
              "distance": { "type": "number" },
              "groundSpeed": { "type": "number" },
              "ete": { "type": "number" },
              "fuel": { "type": "number" }
            }
          }
        },
        "fuelPlan": {
            "type": "object",
            "properties": {
                "tripFuel": { "type": "number" },
                "contingencyFuel": { "type": "number" },
                "alternateFuel": { "type": "number" },
                "finalReserveFuel": { "type": "number" },
                "extraFuel": { "type": "number" },
                "totalFuelRequired": { "type": "number" }
            }
        }
      },
       "required": ["id", "pilotId", "aircraftId", "departure", "destination"]
    },
    "Risk": {
      "title": "Risk",
      "type": "object",
      "description": "Represents an identified organizational risk.",
      "properties": {
        "id": { "type": "string" },
        "hazard": { "type": "string" },
        "risk": { "type": "string" },
        "hazardArea": { "type": "string" },
        "likelihood": { "type": "number" },
        "severity": { "type": "number" },
        "riskScore": { "type": "number" },
        "residualLikelihood": { "type": "number" },
        "residualSeverity": { "type": "number" },
        "residualRiskScore": { "type": "number" },
        "status": { "type": "string", "enum": ["Open", "Closed", "Archived"] },
        "mitigation": { "type": "string" }
      },
      "required": ["id", "hazard", "risk", "hazardArea", "likelihood", "severity", "riskScore", "status"]
    },
    "SafetyReport": {
      "title": "Safety Report",
      "type": "object",
      "description": "Represents a single safety report, from initial filing to final closure.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the safety report." },
        "reportNumber": { "type": "string", "description": "A unique, sequential, human-readable report number (e.g., FLT-001)." },
        "reportType": {
          "type": "string",
          "description": "The category of the event being reported.",
          "enum": ["Flight Operations", "Aircraft Defect", "Ground Operations", "General Safety Concern"]
        },
        "status": {
          "type": "string",
          "description": "The current stage of the report in its lifecycle.",
          "enum": ["Open", "Under Review", "Awaiting Action", "Closed"]
        },
        "submittedBy": { "type": "string", "description": "The user ID of the person who filed the report." },
        "submittedByName": { "type": "string", "description": "The name of the user who filed the report." },
        "submittedAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the report was submitted." },
        "closedDate": { "type": "string", "format": "date-time", "description": "Timestamp of when the report was closed." },
        "isAnonymous": { "type": "boolean", "description": "If true, the submitter's identity is hidden." },
        "eventDate": { "type": "string", "format": "date", "description": "The date the event occurred." },
        "eventTime": { "type": "string", "description": "The time the event occurred." },
        "location": { "type": "string", "description": "The location where the event occurred." },
        "aircraftId": { "type": "string", "description": "The ID of the aircraft involved, if any." },
        "description": { "type": "string", "description": "A detailed narrative of the event." },
        "phaseOfFlight": { "type": "string", "description": "For flight operations, the phase of flight when the event occurred." },
        "systemOrComponent": { "type": "string", "description": "For defect reports, the specific system or component involved." },
        "departmentId": { "type": "string", "description": "The department ID responsible for handling the report." },
        "occurrenceCategory": { "type": "string", "description": "The official ICAO occurrence category assigned during triage." },
        "eventClassification": {
          "type": "string",
          "description": "The final classification of the event.",
          "enum": ["Hazard", "Incident", "Accident"]
        },
        "investigationTeam": {
          "type": "array",
          "description": "List of personnel assigned to investigate the report.",
          "items": { "$ref": "#/backend/entities/InvestigationMember" }
        },
        "initialHazards": {
          "type": "array",
          "description": "Hazards identified during the initial risk assessment.",
          "items": { "$ref": "#/backend/entities/ReportHazard" }
        },
        "investigationTasks": {
          "type": "array",
          "description": "A list of tasks for the investigation team.",
          "items": { "$ref": "#/backend/entities/InvestigationTask" }
        },
        "investigationNotes": { "type": "string", "description": "A summary of the investigation findings and root cause." },
        "discussion": {
          "type": "array",
          "description": "The discussion forum for the investigation team.",
          "items": { "$ref": "#/backend/entities/ReportDiscussionItem" }
        },
        "correctiveActions": {
          "type": "array",
          "description": "The corrective action plan (CAP) to address the root cause.",
          "items": { "$ref": "#/backend/entities/CorrectiveAction" }
        },
        "mitigatedHazards": {
            "type": "array",
            "description": "Re-assessed hazards after corrective actions are implemented.",
            "items": { "$ref": "#/backend/entities/ReportHazard" }
        },
        "signatures": {
          "type": "array",
          "description": "Digital signatures for report closure.",
          "items": { "$ref": "#/backend/entities/ReportSignature" }
        }
      },
      "required": ["id", "reportNumber", "reportType", "status", "submittedAt", "eventDate", "description"]
    },
    "InvestigationMember": {
      "title": "Investigation Member",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "name": { "type": "string" },
        "role": { "type": "string", "enum": ["Lead Investigator", "Team Member", "Technical Expert", "Observer"] }
      },
      "required": ["userId", "name", "role"]
    },
    "ReportHazard": {
      "title": "Report Hazard",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string" },
        "riskAssessment": { "$ref": "#/backend/entities/RiskAssessment" }
      },
      "required": ["id", "description"]
    },
    "RiskAssessment": {
      "title": "Risk Assessment",
      "type": "object",
      "properties": {
        "severity": { "type": "number", "description": "Severity score (1-5)" },
        "likelihood": { "type": "number", "description": "Likelihood score (1-5)" },
        "riskScore": { "type": "number", "description": "Calculated risk score (Severity * Likelihood)" },
        "riskLevel": { "type": "string", "enum": ["Low", "Medium", "High", "Critical"] }
      },
      "required": ["severity", "likelihood", "riskScore", "riskLevel"]
    },
    "InvestigationTask": {
      "title": "Investigation Task",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string" },
        "assigneeId": { "type": "string" },
        "dueDate": { "type": "string", "format": "date" },
        "status": { "type": "string", "enum": ["Open", "In Progress", "Completed"] }
      },
      "required": ["id", "description", "assigneeId", "status"]
    },
    "ReportDiscussionItem": {
      "title": "Report Discussion Item",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "message": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "userName", "message", "timestamp"]
    },
    "CorrectiveAction": {
      "title": "Corrective Action",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string" },
        "responsiblePersonId": { "type": "string" },
        "deadline": { "type": "string", "format": "date" },
        "status": { "type": "string", "enum": ["Open", "In Progress", "Closed", "Cancelled"] }
      },
      "required": ["id", "description", "responsiblePersonId", "status"]
    },
    "ReportSignature": {
      "title": "Report Signature",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "role": { "type": "string", "description": "Role at time of signing (e.g., Safety Manager)" },
        "signatureUrl": { "type": "string", "format": "uri", "description": "Data URL of the signature image." },
        "signedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "userName", "role", "signatureUrl", "signedAt"]
    },
    "ManagementOfChange": {
      "title": "Management of Change",
      "type": "object",
      "description": "Represents a Management of Change (MOC) record.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the MOC." },
        "mocNumber": { "type": "string", "description": "A unique, sequential, human-readable MOC number (e.g., MOC-001)." },
        "title": { "type": "string", "description": "A clear, concise title for the change." },
        "description": { "type": "string", "description": "A detailed explanation of what is changing." },
        "reason": { "type": "string", "description": "The reason why the change is being made." },
        "scope": { "type": "string", "description": "Who and what will be affected by the change." },
        "proposingDepartmentId": { "type": "string", "description": "The ID of the department proposing the change." },
        "responsiblePersonId": { "type": "string", "description": "The ID of the person responsible for the proposal." },
        "proposedBy": { "type": "string", "description": "The user ID of the person who initiated the change." },
        "proposalDate": { "type": "string", "format": "date-time", "description": "Timestamp of when the MOC was proposed." },
        "status": {
          "type": "string",
          "enum": ["Proposed", "Under Review", "Approved", "In Progress", "Closed", "Cancelled"]
        },
        "phases": {
          "type": "array",
          "description": "The major stages of implementing the change.",
          "items": { "$ref": "#/backend/entities/MocPhase" }
        },
        "signatures": {
          "type": "array",
          "description": "Digital signatures for MOC approval.",
          "items": { "$ref": "#/backend/entities/MocSignature" }
        }
      },
      "required": ["id", "mocNumber", "title", "status", "proposalDate", "proposingDepartmentId"]
    },
    "MocPhase": {
      "title": "MOC Phase",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "steps": { "type": "array", "items": { "$ref": "#/backend/entities/MocStep" } }
      },
      "required": ["id", "title"]
    },
    "MocStep": {
      "title": "MOC Step",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string" },
        "hazards": { "type": "array", "items": { "$ref": "#/backend/entities/MocHazard" } }
      },
      "required": ["id", "description"]
    },
    "MocHazard": {
      "title": "MOC Hazard",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string" },
        "risks": { "type": "array", "items": { "$ref": "#/backend/entities/MocRisk" } }
      },
      "required": ["id", "description"]
    },
    "MocRisk": {
      "title": "MOC Risk",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string" },
        "initialRiskAssessment": { "$ref": "#/backend/entities/RiskAssessment" },
        "mitigations": { "type": "array", "items": { "$ref": "#/backend/entities/MocMitigation" } }
      },
      "required": ["id", "description"]
    },
    "MocMitigation": {
      "title": "MOC Mitigation",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "description": { "type": "string" },
        "responsiblePersonId": { "type": "string" },
        "completionDate": { "type": "string", "format": "date" },
        "status": { "type": "string", "enum": ["Open", "In Progress", "Closed", "Cancelled"] },
        "residualRiskAssessment": { "$ref": "#/backend/entities/RiskAssessment" }
      },
      "required": ["id", "description", "status"]
    },
    "MocSignature": {
      "title": "MOC Signature",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "role": { "type": "string", "description": "Role at time of signing" },
        "signatureUrl": { "type": "string", "format": "uri" },
        "signedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["userId", "userName", "role", "signatureUrl", "signedAt"]
    },
    "Booking": {
      "title": "Booking",
      "type": "object",
      "description": "Represents a booking for an aircraft.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the booking."
        },
        "createdById": {
            "type": "string",
            "description": "The user ID of the person who created the booking."
        },
        "flightPlanId": {
          "type": "string",
          "description": "The ID of the flight plan associated with this booking."
        },
        "bookingNumber": {
            "type": "number",
            "description": "A sequential, human-readable booking number."
        },
        "aircraftId": {
          "type": "string",
          "description": "The ID of the booked aircraft."
        },
        "studentId": {
            "type": "string",
            "description": "The ID of the student for a training flight."
        },
        "instructorId": {
            "type": "string",
            "description": "The ID of the instructor for a training flight."
        },
        "type": {
          "type": "string",
          "description": "The type of booking.",
          "enum": ["Training Flight", "Private Flight", "Reposition Flight", "Maintenance Flight"]
        },
        "date": {
          "type": "string",
          "format": "date",
          "description": "The date of the booking (YYYY-MM-DD)."
        },
        "startTime": {
          "type": "string",
          "description": "The start time of the booking (HH:mm)."
        },
        "endTime": {
          "type": "string",
          "description": "The end time of the booking (HH:mm)."
        },
        "status": {
            "type": "string",
            "description": "The status of the booking.",
            "enum": ["Confirmed", "Completed", "Cancelled", "Cancelled with Reason"]
        },
        "cancellationReason": {
            "type": "string",
            "description": "The reason a booking was cancelled."
        },
        "isOvernight": {
            "type": "boolean",
            "description": "Flag to indicate if the booking spans across midnight."
        },
        "overnightBookingDate": {
            "type": "string",
            "format": "date",
            "description": "The date for the second part of an overnight booking (YYYY-MM-DD)."
        },
        "overnightEndTime": {
            "type": "string",
            "description": "The end time for the second part of an overnight booking (HH:mm)."
        },
        "flightDetails": {
            "type": "string",
            "description": "Details about the flight, such as route or purpose."
        },
        "preFlight": {
            "type": "object",
            "description": "Pre-flight checklist data.",
            "properties": {
                "actualHobbs": { "type": "number" },
                "actualTacho": { "type": "number" },
                "oil": { "type": "number" },
                "fuel": { "type": "number" },
                "documentsChecked": { "type": "array", "items": { "type": "string" } },
                "photos": {
                    "type": "array",
                    "description": "Photos taken during pre-flight.",
                    "items": {
                        "type": "object",
                        "properties": {
                            "url": { "type": "string", "description": "Data URL of the photo." },
                            "description": { "type": "string", "description": "A brief description of the photo." }
                        }
                    }
                }
            }
        },
        "postFlight": {
            "type": "object",
            "description": "Post-flight checklist data.",
            "properties": {
                "actualHobbs": { "type": "number" },
                "actualTacho": { "type": "number" },
                "oil": { "type": "number" },
                "fuel": { "type": "number" },
                "photos": {
                    "type": "array",
                    "description": "Photos taken during post-flight.",
                    "items": {
                        "type": "object",
                        "properties": {
                            "url": { "type": "string", "description": "Data URL of the photo." },
                            "description": { "type": "string", "description": "A brief description of the photo." }
                        }
                    }
                }
            }
        },
        "massAndBalance": {
            "type": "object",
            "description": "Mass and Balance calculation data for the flight.",
            "properties": {
                "stationWeights": {
                    "type": "object",
                    "description": "A map of station IDs to their weights for this flight.",
                    "additionalProperties": { "type": "number" }
                },
                "totalWeight": { "type": "number" },
                "totalMoment": { "type": "number" },
                "centerOfGravity": { "type": "number" },
                "isWithinLimits": { "type": "boolean" },
                "calculatedAt": { "type": "string", "format": "date-time" }
            }
        }
      },
      "required": ["id", "aircraftId", "type", "date", "startTime", "endTime"]
    },
    "LogbookTemplate": {
      "title": "Logbook Template",
      "type": "object",
      "description": "Represents a saved structure for a flight logbook.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string", "description": "The user-defined name for this template." },
        "columns": {
          "type": "array",
          "description": "The structured columns of the logbook table, allowing for nesting.",
          "items": { "$ref": "#/backend/entities/LogbookColumn" }
        }
      },
      "required": ["id", "name", "columns"]
    },
    "LogbookColumn": {
        "title": "Logbook Column",
        "type": "object",
        "properties": {
            "id": { "type": "string" },
            "label": { "type": "string" },
            "subColumns": {
                "type": "array",
                "items": { "$ref": "#/backend/entities/LogbookColumn" }
            }
        },
        "required": ["id", "label"]
    },
    "AircraftModelProfile": {
      "title": "Aircraft Model Profile",
      "type": "object",
      "description": "Represents a mass and balance template for a specific aircraft make and model.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the profile." },
        "profileName": { "type": "string", "description": "The unique name for this M&B profile." },
        "emptyWeight": { "type": "number", "description": "The standard empty weight of this aircraft model (in lbs)." },
        "emptyWeightMoment": { "type": "number", "description": "The standard moment of this model at its empty weight (in lbs-in)." },
        "maxTakeoffWeight": { "type": "number", "description": "Maximum allowable takeoff weight (in lbs)." },
        "maxLandingWeight": { "type": "number", "description": "Maximum allowable landing weight (in lbs)." },
        "stationArms": {
          "type": "object",
          "description": "Defines the arm (distance from datum in inches) for each station.",
          "properties": {
            "frontSeats": { "type": "number", "description": "Arm for the front seats." },
            "rearSeats": { "type": "number", "description": "Arm for the rear seats." },
            "fuel": { "type": "number", "description": "Arm for the fuel tank(s)." },
            "baggage1": { "type": "number", "description": "Arm for the primary baggage area." },
            "baggage2": { "type": "number", "description": "Arm for the secondary baggage area, if applicable." }
          }
        },
        "stations": {
          "type": "array",
          "description": "A list of loading stations for the aircraft model.",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "number" },
              "name": { "type": "string" },
              "weight": { "type": "number" },
              "arm": { "type": "number" },
              "type": { "type": "string" },
              "gallons": { "type": "number" },
              "maxGallons": { "type": "number" }
            }
          }
        },
        "cgEnvelope": {
          "type": "array",
          "description": "An array of {x, y} objects defining the vertices of the CG envelope polygon.",
          "items": {
            "type": "object",
            "properties": {
              "x": { "type": "number", "description": "Center of Gravity (inches)" },
              "y": { "type": "number", "description": "Weight (lbs)" }
            },
            "required": ["x", "y"]
          }
        },
        "xMin": { "type": "number", "description": "Minimum value for the CG chart's X-axis (CG)." },
        "xMax": { "type": "number", "description": "Maximum value for the CG chart's X-axis (CG)." },
        "yMin": { "type": "number", "description": "Minimum value for the CG chart's Y-axis (Weight)." },
        "yMax": { "type": "number", "description": "Maximum value for the CG chart's Y-axis (Weight)." }
      },
      "required": ["id", "profileName"]
    },
    "PilotProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "PilotProfile",
      "type": "object",
      "description": "Represents a pilot's profile, including students, private pilots, and instructors.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the PilotProfile entity."
        },
        "userType": {
          "type": "string",
          "description": "The category of the user.",
          "enum": ["Student", "Private Pilot", "Instructor"]
        },
        "firstName": {
          "type": "string",
          "description": "Pilot's first name."
        },
        "lastName": {
          "type": "string",
          "description": "Pilot's last name."
        },
        "email": {
          "type": "string",
          "description": "Pilot's email address.",
          "format": "email"
        },
        "role": { "type": "string", "description": "The ID of the role assigned to the user." },
        "contactNumber": { "type": "string", "description": "The contact phone number of the pilot." },
        "dateOfBirth": { "type": "string", "description": "The pilot's date of birth.", "format": "date" },
        "logbookTemplateId": { "type": "string", "description": "The ID of the logbook template assigned to this pilot." },
        "address": { "$ref": "#/backend/entities/Address" },
        "emergencyContact": { "$ref": "#/backend/entities/EmergencyContact" },
        "documents": { "$ref": "#/backend/entities/Documents" },
        "pilotLicense": { "$ref": "#/backend/entities/PilotLicense" }
      },
      "required": ["id", "userType", "firstName", "lastName", "email"]
    },
    "Personnel": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Personnel",
      "type": "object",
      "description": "Represents a non-flying staff member at the aviation academy.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Personnel entity."
        },
        "userType": {
          "type": "string",
          "description": "The category of the user, fixed to 'Personnel'.",
          "enum": ["Personnel"]
        },
        "firstName": {
          "type": "string",
          "description": "Personnel's first name."
        },
        "lastName": {
          "type": "string",
          "description": "Personnel's last name."
        },
        "role": {
          "type": "string",
          "description": "The ID of the role assigned to the personnel."
        },
        "email": {
          "type": "string",
          "description": "Personnel's email address.",
          "format": "email"
        },
        "permissions": {
          "type": "array",
          "description": "An array of permission IDs (strings) associated with this person, potentially overriding the role.",
          "items": { "type": "string" }
        },
        "department": {
          "type": "string",
          "description": "The ID of the department the personnel belongs to."
        },
        "contactNumber": { "type": "string", "description": "The contact phone number of the personnel." },
        "dateOfBirth": { "type": "string", "description": "The personnel's date of birth.", "format": "date" },
        "address": { "$ref": "#/backend/entities/Address" },
        "emergencyContact": { "$ref": "#/backend/entities/EmergencyContact" },
        "documents": { "$ref": "#/backend/entities/Documents" }
      },
      "required": ["id", "userType", "firstName", "lastName", "role", "email"]
    },
    "PilotLicense": {
        "title": "PilotLicense",
        "type": "object",
        "description": "Represents a pilot's license details.",
        "properties": {
            "licenseNumber": { "type": "string", "description": "The pilot's license number." },
            "issueDate": { "type": "string", "format": "date", "description": "The date the license was issued." },
            "expirationDate": { "type": "string", "format": "date", "description": "The date the license expires." },
            "ratings": { 
                "type": "array", 
                "description": "List of ratings held by the pilot.",
                "items": { "type": "string" }
            },
            "endorsements": { 
                "type": "array", 
                "description": "List of endorsements held by the pilot.",
                "items": { "type": "string" }
            }
        }
    },
    "Address": {
        "title": "Address",
        "type": "object",
        "description": "Represents a physical address.",
        "properties": {
            "street": { "type": "string" },
            "city": { "type": "string" },
            "state": { "type": "string" },
            "postalCode": { "type": "string" },
            "country": { "type": "string" }
        }
    },
    "EmergencyContact": {
        "title": "EmergencyContact",
        "type": "object",
        "description": "Represents an emergency contact.",
        "properties": {
            "name": { "type": "string" },
            "relationship": { "type": "string" },
            "phone": { "type": "string" }
        }
    },
    "Documents": {
        "title": "Documents",
        "type": "array",
        "description": "A list of documents associated with a user or aircraft.",
        "items": {
            "type": "object",
            "properties": {
                "name": { "type": "string" },
                "url": { "type": "string", "format": "uri" },
                "uploadDate": { "type": "string", "format": "date-time" },
                "expirationDate": { "type": "string", "format": "date" },
                "abbreviation": { "type": "string", "description": "A 5-character abbreviation for the document." }
            }
        }
    },
    "Aircraft": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Aircraft",
      "type": "object",
      "description": "Represents an aircraft used for training.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Aircraft entity."
        },
        "tailNumber": {
          "type": "string",
          "description": "Aircraft's tail number."
        },
        "model": {
          "type": "string",
          "description": "Aircraft's model."
        },
        "abbreviation": {
          "type": "string",
          "description": "A 5-character abbreviation for the aircraft."
        },
        "type": {
          "type": "string",
          "description": "The type of aircraft.",
          "enum": [
            "Single-Engine",
            "Multi-Engine"
          ]
        },
        "frameHours": {
          "type": "number",
          "description": "Total hours on the aircraft frame."
        },
        "engineHours": {
          "type": "number",
          "description": "Total hours on the engine."
        },
        "initialHobbs": {
          "type": "number",
          "description": "Initial Hobbs meter reading."
        },
        "currentHobbs": {
          "type": "number",
          "description": "Current Hobbs meter reading."
        },
        "initialTacho": {
          "type": "number",
          "description": "Initial Tachometer reading."
        },
        "currentTacho": {
          "type": "number",
          "description": "Current Tachometer reading."
        },
        "tachoAtNext50Inspection": {
          "type": "number",
          "description": "Tacho reading at which the next 50-hour inspection is due."
        },
        "tachoAtNext100Inspection": {
          "type": "number",
          "description": "Tacho reading at which the next 100-hour inspection is due."
        },
        "maintenanceLogs": {
          "type": "array",
          "description": "References to MaintenanceLog entries. (Relationship: Aircraft 1:N MaintenanceLog)",
          "items": {
            "type": "string"
          }
        },
        "documents": {
           "$ref": "#/backend/entities/Documents"
        },
        "emptyWeight": {
          "type": "number",
          "description": "The basic empty weight of the aircraft (in lbs)."
        },
        "emptyWeightMoment": {
          "type": "number",
          "description": "The moment of the aircraft at its basic empty weight (in lbs-in)."
        },
        "maxTakeoffWeight": {
          "type": "number",
          "description": "Maximum allowable takeoff weight (in lbs)."
        },
        "maxLandingWeight": {
          "type": "number",
          "description": "Maximum allowable landing weight (in lbs)."
        },
        "stationArms": {
          "type": "object",
          "description": "Defines the arm (distance from datum in inches) for each station.",
          "properties": {
            "frontSeats": { "type": "number", "description": "Arm for the front seats." },
            "rearSeats": { "type": "number", "description": "Arm for the rear seats." },
            "fuel": { "type": "number", "description": "Arm for the fuel tank(s)." },
            "baggage1": { "type": "number", "description": "Arm for the primary baggage area." },
            "baggage2": { "type": "number", "description": "Arm for the secondary baggage area." }
          }
        },
        "cgEnvelope": {
          "type": "array",
          "description": "An array of {weight, cg} objects defining the vertices of the CG envelope polygon.",
          "items": {
            "type": "object",
            "properties": {
              "weight": { "type": "number", "description": "Weight (lbs)" },
              "cg": { "type": "number", "description": "Center of Gravity (inches)" }
            }
          }
        }
      },
      "required": [
        "id",
        "tailNumber",
        "model"
      ]
    },
    "MaintenanceLog": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MaintenanceLog",
      "type": "object",
      "description": "Represents a maintenance log entry for an aircraft.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the MaintenanceLog entity."
        },
        "aircraftId": {
          "type": "string",
          "description": "Reference to Aircraft. (Relationship: Aircraft 1:N MaintenanceLog)"
        },
        "date": {
          "type": "string",
          "description": "Date of the maintenance.",
          "format": "date-time"
        },
        "description": {
          "type": "string",
          "description": "Description of the maintenance performed."
        },
        "procedure": {
          "type": "string",
          "description": "Details of the maintenance procedure followed."
        }
      },
      "required": [
        "id",
        "aircraftId",
        "date",
        "description"
      ]
    },
    "ComplianceCheck": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ComplianceCheck",
      "type": "object",
      "description": "Represents a compliance check performed.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the ComplianceCheck entity."
        },
        "safetyProtocolId": {
          "type": "string",
          "description": "Reference to SafetyProtocol. (Relationship: SafetyProtocol 1:N ComplianceCheck)"
        },
        "date": {
          "type": "string",
          "description": "Date when the compliance check was performed.",
          "format": "date-time"
        },
        "result": {
          "type": "boolean",
          "description": "Result of the compliance check (true for pass, false for fail)."
        },
        "notes": {
          "type": "string",
          "description": "Additional notes or observations from the compliance check."
        }
      },
      "required": [
        "id",
        "safetyProtocolId",
        "date",
        "result"
      ]
    },
    "Permission": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Permission",
      "type": "object",
      "description": "Represents a permission within the application, typically generated from the menu structure.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Permission entity, often derived from the menu href."
        },
        "name": {
          "type": "string",
          "description": "The human-readable name of the permission (e.g., 'Users')."
        },
        "description": {
          "type": "string",
          "description": "A description of what the permission grants."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Role": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Role",
      "type": "object",
      "description": "Represents a role within the application, defining a set of permissions.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Role entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the role."
        },
        "permissions": {
          "type": "array",
          "description": "An array of permission IDs (strings) associated with this role.",
          "items": { "type": "string" }
        },
        "requiredDocuments": {
            "type": "array",
            "description": "A list of document names that a user with this role is required to upload.",
            "items": {
                "type": "string"
            }
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Tenant": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Tenant",
      "type": "object",
      "description": "Represents a Tenant on the system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Tenant."
        },
        "name": {
          "type": "string",
          "description": "Name of the tenant"
        },
        "logoUrl": {
          "type": "string",
          "description": "URL of the company logo."
        },
        "theme": {
          "$ref": "#/backend/entities/Theme"
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Theme": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Theme",
      "type": "object",
      "description": "Represents the application theme for a tenant.",
      "properties": {
        "primaryColour": {
          "type": "string",
          "description": "The primary colour to use"
        },
        "backgroundColour": {
          "type": "string",
          "description": "The background colour to use"
        },
        "accentColour": {
          "type": "string",
          "description": "The accent colour to use"
        }
      }
    },
    "Settings": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Settings",
      "type": "object",
      "description": "Represents the application settings for a tenant.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Settings."
        },
        "tenantId": {
          "type": "string",
          "description": "The tenant this setting belongs to."
        },
        "theme": {
          "$ref": "#/backend/entities/Theme"
        },
        "documentExpiry": {
            "$ref": "#/backend/entities/DocumentExpiry"
        },
        "findingLevels": {
            "$ref": "#/backend/entities/FindingLevelsSettings"
        }
      },
      "required": [
        "id",
        "tenantId"
      ]
    },
    "FeatureSettings": {
      "title": "Feature Settings",
      "type": "object",
      "description": "Represents tenant-wide feature flags and settings.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the feature settings document." },
        "preFlightChecklistRequired": { "type": "boolean", "description": "If true, pre-flight checklists are mandatory and block subsequent bookings if not completed." }
      }
    },
    "WarningPeriod": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "WarningPeriod",
        "type": "object",
        "description": "Represents a single document expiry warning period with its associated color.",
        "properties": {
            "period": {
                "type": "number",
                "description": "The number of days before expiration to send a warning."
            },
            "color": {
                "type": "string",
                "description": "The hex color code associated with this warning period."
            }
        },
        "required": ["period", "color"]
    },
    "DocumentExpiry": {
        "$schema": "http://json-schema.org/draft-07/schema#",
        "title": "DocumentExpiry",
        "type": "object",
        "description": "Represents the document expiry warning settings for a tenant.",
        "properties": {
            "defaultColor": {
                "type": "string",
                "description": "The hex color code for documents not within any warning period."
            },
            "expiredColor": {
                "type": "string",
                "description": "The hex color code for documents that have expired."
            },
            "warningPeriods": {
                "type": "array",
                "description": "An array of warning period objects.",
                "items": {
                    "$ref": "#/backend/entities/WarningPeriod"
                }
            }
        },
        "required": ["warningPeriods"]
    },
    "Department": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Department",
      "type": "object",
      "description": "Represents a department within a tenant.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the department."
        },
        "name": {
          "type": "string",
          "description": "Name of the department."
        }
      },
      "required": [
        "id",
        "name"
      ]
    },
    "Counter": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Counter",
      "type": "object",
      "description": "Represents a distributed counter.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the counter (e.g., 'bookings')."
        },
        "currentNumber": {
          "type": "number",
          "description": "The last number that was used."
        }
      },
      "required": ["id", "currentNumber"]
    },
    "AuditScheduleItem": {
        "title": "Audit Schedule Item",
        "type": "object",
        "description": "Represents the status of a single audit area for a specific quarter and year.",
        "properties": {
            "id": { "type": "string" },
            "area": { "type": "string" },
            "quarter": { "type": "string" },
            "year": { "type": "number" },
            "status": { "type": "string", "enum": ["Scheduled", "Completed", "Pending", "Not Scheduled"] }
        },
        "required": ["id", "area", "quarter", "year", "status"]
    },
    "QualityAuditChecklistTemplate": {
        "title": "Quality Audit Checklist Template",
        "type": "object",
        "description": "A reusable template for conducting quality audits.",
        "properties": {
            "id": { "type": "string" },
            "title": { "type": "string" },
            "departmentId": { "type": "string" },
            "sections": {
                "type": "array",
                "items": { "$ref": "#/backend/entities/ChecklistSection" }
            }
        },
        "required": ["id", "title", "departmentId"]
    },
    "ChecklistSection": {
        "title": "Checklist Section",
        "type": "object",
        "description": "A section within a quality audit checklist template.",
        "properties": {
            "id": { "type": "string" },
            "title": { "type": "string" },
            "items": {
                "type": "array",
                "items": { "$ref": "#/backend/entities/AuditChecklistItem" }
            }
        },
        "required": ["id", "title"]
    },
    "AuditChecklistItem": {
        "title": "Audit Checklist Item",
        "type": "object",
        "description": "A single item within a quality audit checklist section.",
        "properties": {
            "id": { "type": "string" },
            "text": { "type": "string" },
            "type": { "type": "string", "enum": ["Checkbox", "Textbox", "Number", "Date"] },
            "regulationReference": { "type": "string" }
        },
        "required": ["id", "text", "type"]
    },
    "QualityAudit": {
        "title": "Quality Audit",
        "type": "object",
        "description": "An instance of a quality audit performed using a template.",
        "properties": {
            "id": { "type": "string" },
            "templateId": { "type": "string" },
            "title": { "type": "string" },
            "auditNumber": { "type": "string" },
            "auditorId": { "type": "string" },
            "auditeeId": { "type": "string" },
            "scope": { "type": "string" },
            "auditDate": { "type": "string", "format": "date-time" },
            "status": { "type": "string", "enum": ["Scheduled", "In Progress", "Finalized", "Closed", "Archived"] },
            "findings": {
                "type": "array",
                "items": { "$ref": "#/backend/entities/QualityFinding" }
            },
            "complianceScore": { "type": "number" }
        },
        "required": ["id", "templateId", "title", "auditNumber", "auditorId", "status", "auditDate"]
    },
    "QualityFinding": {
        "title": "Quality Finding",
        "type": "object",
        "description": "A specific finding recorded during a quality audit.",
        "properties": {
            "checklistItemId": { "type": "string" },
            "finding": { "type": "string", "enum": ["Compliant", "Non Compliant", "Not Applicable"] },
            "level": { "type": "string" },
            "comment": { "type": "string" },
            "suggestedImprovements": { "type": "string", "description": "Suggested improvements related to the finding." },
            "evidence": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "url": { "type": "string" },
                        "description": { "type": "string" }
                    }
                }
            }
        },
        "required": ["checklistItemId", "finding"]
    },
    "CorrectiveActionPlan": {
        "title": "Corrective Action Plan",
        "type": "object",
        "description": "A plan to address a non-conformance identified in an audit.",
        "properties": {
            "id": { "type": "string" },
            "auditId": { "type": "string" },
            "findingId": { "type": "string" },
            "rootCauseAnalysis": { "type": "string" },
            "actions": {
                "type": "array",
                "items": { "$ref": "#/backend/entities/CorrectiveAction" }
            }
        },
        "required": ["id", "auditId", "findingId", "rootCauseAnalysis"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
       {
        "path": "tenants/{tenantId}/published-tables/{pageId}",
        "definition": {
          "entityName": "PublishedTable",
          "schema": { "$ref": "#/backend/entities/PublishedTable" },
          "description": "Stores table structures published to specific pages."
        }
      },
      {
        "path": "tenants/{tenantId}/table-templates/{templateId}",
        "definition": {
          "entityName": "TableTemplate",
          "schema": { "$ref": "#/backend/entities/TableTemplate" },
          "description": "Stores saved table designs from the Table Builder."
        }
      },
       {
        "path": "tenants/{tenantId}/compliance-matrix/{itemId}",
        "definition": {
          "entityName": "ComplianceRequirement",
          "schema": { "$ref": "#/backend/entities/ComplianceRequirement" },
          "description": "Stores individual regulatory compliance items."
        }
      },
      {
        "path": "tenants/{tenantId}/audit-schedule-items/{itemId}",
        "definition": {
          "entityName": "AuditScheduleItem",
          "schema": { "$ref": "#/backend/entities/AuditScheduleItem" },
          "description": "Stores the status of audit areas for specific quarters."
        }
      },
      {
        "path": "tenants/{tenantId}/quality-audit-templates/{templateId}",
        "definition": {
          "entityName": "QualityAuditChecklistTemplate",
          "schema": { "$ref": "#/backend/entities/QualityAuditChecklistTemplate" },
          "description": "Stores templates for quality audits."
        }
      },
      {
        "path": "tenants/{tenantId}/quality-audits/{auditId}",
        "definition": {
          "entityName": "QualityAudit",
          "schema": { "$ref": "#/backend/entities/QualityAudit" },
          "description": "Stores instances of quality audits."
        }
      },
      {
        "path": "tenants/{tenantId}/corrective-action-plans/{capId}",
        "definition": {
          "entityName": "CorrectiveActionPlan",
          "schema": { "$ref": "#/backend/entities/CorrectiveActionPlan" },
          "description": "Stores corrective action plans for audit findings."
        }
      },
       {
        "path": "tenants/{tenantId}/flight-plans/{flightPlanId}",
        "definition": {
          "entityName": "FlightPlan",
          "schema": {
            "$ref": "#/backend/entities/FlightPlan"
          },
          "description": "Stores flight plans for each tenant.",
          "params": [
            { "name": "tenantId", "description": "Tenant ID." },
            { "name": "flightPlanId", "description": "Flight Plan ID." }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/risks/{riskId}",
        "definition": {
          "entityName": "Risk",
          "schema": {
            "$ref": "#/backend/entities/Risk"
          },
          "description": "Stores organizational risks for each tenant.",
          "params": [
            { "name": "tenantId", "description": "Tenant ID." },
            { "name": "riskId", "description": "Risk ID." }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/safety-reports/{reportId}",
        "definition": {
          "entityName": "SafetyReport",
          "schema": {
            "$ref": "#/backend/entities/SafetyReport"
          },
          "description": "Stores safety reports for each tenant.",
          "params": [
            { "name": "tenantId", "description": "Tenant ID." },
            { "name": "reportId", "description": "Safety Report ID." }
          ]
        }
      },
       {
        "path": "tenants/{tenantId}/management-of-change/{mocId}",
        "definition": {
          "entityName": "ManagementOfChange",
          "schema": {
            "$ref": "#/backend/entities/ManagementOfChange"
          },
          "description": "Stores Management of Change records for each tenant.",
          "params": [
            { "name": "tenantId", "description": "Tenant ID." },
            { "name": "mocId", "description": "MOC ID." }
          ]
        }
      },
       {
        "path": "tenants/{tenantId}/bookings/{bookingId}",
        "definition": {
          "entityName": "Booking",
          "schema": {
            "$ref": "#/backend/entities/Booking"
          },
          "description": "Stores individual booking records for each tenant.",
          "params": [
            { "name": "tenantId", "description": "Tenant ID." },
            { "name": "bookingId", "description": "Booking ID." }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/logbook-templates/{templateId}",
        "definition": {
          "entityName": "LogbookTemplate",
          "schema": {
            "$ref": "#/backend/entities/LogbookTemplate"
          },
          "description": "Stores saved structures for flight logbooks.",
          "params": [
            { "name": "tenantId", "description": "Tenant ID." },
            { "name": "templateId", "description": "Template ID." }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/massAndBalance/{profileId}",
        "definition": {
          "entityName": "AircraftModelProfile",
          "schema": {
            "$ref": "#/backend/entities/AircraftModelProfile"
          },
          "description": "Stores M&B templates for aircraft models.",
          "params": [
            { "name": "tenantId", "description": "Tenant ID." },
            { "name": "profileId", "description": "Profile ID." }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}",
        "definition": {
          "entityName": "Tenant",
          "schema": {
            "$ref": "#/backend/entities/Tenant"
          },
          "description": "Stores tenant information. All other collections except users are tenant specific.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/personnel/{personnelId}",
        "definition": {
          "entityName": "Personnel",
          "schema": {
            "$ref": "#/backend/entities/Personnel"
          },
          "description": "Stores non-flying staff information, scoped to a specific tenant.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "personnelId",
              "description": "Unique identifier for the personnel."
            }
          ]
        }
      },
       {
        "path": "tenants/{tenantId}/pilots/{pilotId}",
        "definition": {
          "entityName": "PilotProfile",
          "schema": {
            "$ref": "#/backend/entities/PilotProfile"
          },
          "description": "Stores pilot profile information (students, instructors, private pilots), scoped to a specific tenant.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "pilotId",
              "description": "Unique identifier for the pilot profile."
            }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/aircrafts/{aircraftId}",
        "definition": {
          "entityName": "Aircraft",
          "schema": {
            "$ref": "#/backend/entities/Aircraft"
          },
          "description": "Stores aircraft information, scoped to a specific tenant.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "aircraftId",
              "description": "Unique identifier for the aircraft."
            }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/aircrafts/{aircraftId}/maintenanceLogs/{maintenanceLogId}",
        "definition": {
          "entityName": "MaintenanceLog",
          "schema": {
            "$ref": "#/backend/entities/MaintenanceLog"
          },
          "description": "Stores maintenance log entries for a specific aircraft, scoped to a specific tenant.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "aircraftId",
              "description": "Unique identifier for the aircraft."
            },
            {
              "name": "maintenanceLogId",
              "description": "Unique identifier for the maintenance log entry."
            }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/roles/{roleId}",
        "definition": {
          "entityName": "Role",
          "schema": {
            "$ref": "#/backend/entities/Role"
          },
          "description": "Stores roles, scoped to a specific tenant.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "roleId",
              "description": "Unique identifier for the role."
            }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/permissions/{permissionId}",
        "definition": {
          "entityName": "Permission",
          "schema": {
            "$ref": "#/backend/entities/Permission"
          },
          "description": "Stores all available application permissions, scoped to a specific tenant.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "permissionId",
              "description": "Unique identifier for the permission."
            }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/settings/{settingsId}",
        "definition": {
          "entityName": "Settings",
          "schema": {
            "$ref": "#/backend/entities/Settings"
          },
          "description": "Stores application settings for a tenant. A single document can hold multiple settings, e.g., 'document-expiry', 'features', 'finding-levels'.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "settingsId",
              "description": "Unique identifier for the settings document (e.g., 'theme', 'document-expiry', 'features', 'finding-levels')."
            }
          ]
        }
      },
       {
        "path": "tenants/{tenantId}/departments/{departmentId}",
        "definition": {
          "entityName": "Department",
          "schema": {
            "$ref": "#/backend/entities/Department"
          },
          "description": "Stores departments, scoped to a specific tenant.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "departmentId",
              "description": "Unique identifier for the department."
            }
          ]
        }
      },
      {
        "path": "tenants/{tenantId}/counters/{counterId}",
        "definition": {
          "entityName": "Counter",
          "schema": {
            "$ref": "#/backend/entities/Counter"
          },
          "description": "Stores distributed counters for generating sequential IDs.",
          "params": [
            {
              "name": "tenantId",
              "description": "Unique identifier for the tenant."
            },
            {
              "name": "counterId",
              "description": "The name of the counter (e.g., 'bookings')."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to support a multi-tenant aviation academy management application, focusing on security, scalability, and ease of debugging. Authorization Independence is achieved through denormalization where needed, especially for collaborative data. Structural Segregation ensures that each collection has a homogeneous security posture. Access Modeling follows consistent patterns, utilizing path-based ownership for user-specific data and membership maps for collaborative entities. The structure is multi-tenant, where tenants are isolated from each other. The structure uses the tenants collection to keep a list of tenants and provides a tenant id for all other collections in the database. This strategy allows the application to manage multiple academies within the same system, each with its own set of data and settings.\\n\\n*   **Tenants:** The root level collection `tenants` stores individual tenant documents. Each tenant has its own unique ID and associated settings.\\n*   **Users:** User data is now split into `personnel` (non-flying staff), `instructors`, `students`, and `private-pilots` under each tenant to better segregate data models and security rules.\\n*   **Settings:** A generic `settings` collection is added under each tenant. This allows for storing various configuration documents, such as `document-expiry`, in a structured and scalable way without creating new top-level collections for each setting type.\\n\\nQAPs (Rules are not Filters) is addressed by:\\n\\n*   Segregating data into collections with homogeneous security requirements.\\n*   Using path-based ownership for private data.\\n*   Employing membership maps for collaborative data, and storing these maps directly within the documents to avoid `get()` calls in security rules.\\n\\nThis design enables robust security rules that are simple to write, easy to understand, and highly performant."
  }
}
