'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function PerformChecklist() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Perform Checklist</CardTitle>
            </CardHeader>
            <CardContent>
                <p className="text-muted-foreground">Functionality to perform a checklist will be available here.</p>
            </CardContent>
        </Card>
    );
}
