generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DATABASE_URL_UNPOOLED")
}

model Tenant {
  id                    String                 @id @db.VarChar(128)
  name                  String
  createdAt             DateTime               @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt             DateTime               @default(now()) @map("updated_at") @db.Timestamptz(6)
  users                 User[]
  roles                 Role[]
  departments           Department[]
  personnel             Personnel[]
  tenantConfigs         TenantConfig[]
  managementOfChanges   ManagementOfChange[]
  qualityAudits         QualityAudit[]
  safetyReports         SafetyReport[]
  correctiveActionPlans CorrectiveActionPlan[]
  risks                 Risk[]
  aircrafts             AircraftRecord[]
  vehicles              VehicleRecord[]
  vehicleUsageRecords   VehicleUsageRecord[]
  bookings              BookingRecord[]
  trainingRoutes        TrainingRouteRecord[]
  companyDocuments      CompanyDocument[]
  externalOrganizations ExternalOrganization[]
  erpStates             ErpState[]
  workpacks             WorkpackRecord[]
  maintenanceTaskCards  MaintenanceTaskCardRecord[]
  tools                 ToolRecord[]
  activeFlightSessions  ActiveFlightSessionRecord[]
  alerts                AlertRecord[]

  @@map("tenants")
}

model User {
  id           String   @id @db.VarChar(128)
  tenantId     String   @map("tenant_id") @db.VarChar(128)
  email        String   @unique
  passwordHash String?  @map("password_hash")
  firstName    String   @default("") @map("first_name")
  lastName     String   @default("") @map("last_name")
  role         String   @default("developer")
  profilePath  String?  @map("profile_path")
  createdAt    DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt    DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant       Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("users")
}

model Role {
  id                String   @id @db.VarChar(128)
  tenantId          String   @map("tenant_id") @db.VarChar(128)
  name              String
  permissions       Json     @default("[]")
  requiredDocuments Json?    @map("required_documents")
  createdAt         DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant            Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("roles")
}

model Department {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  name      String
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("departments")
}

model Personnel {
  id                   String   @id @db.VarChar(128)
  tenantId             String   @map("tenant_id") @db.VarChar(128)
  userNumber           String?  @map("user_number")
  firstName            String   @map("first_name")
  lastName             String   @map("last_name")
  email                String
  contactNumber        String?  @map("contact_number")
  organizationId       String?  @map("organization_id")
  department           String?
  role                 String
  permissions          Json     @default("[]")
  accessOverrides      Json?    @map("access_overrides")
  userType             String   @default("Personnel") @map("user_type")
  isErpIncerfaContact  Boolean  @default(false) @map("is_erp_incerfa_contact")
  isErpAlerfaContact   Boolean  @default(false) @map("is_erp_alerfa_contact")
  createdAt            DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant               Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("personnel")
}

model TenantConfig {
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json     @default("{}")
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@id([tenantId])
  @@map("tenant_configs")
}

model ManagementOfChange {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("management_of_change")
}

model QualityAudit {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("quality_audits")
}

model SafetyReport {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("safety_reports")
}

model CorrectiveActionPlan {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("corrective_action_plans")
}

model Risk {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("risks")
}

model AircraftRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("aircrafts")
}

model VehicleRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("vehicles")
}

model VehicleUsageRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("vehicle_usage_records")
}

model BookingRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("bookings")
}

model TrainingRouteRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("training_routes")
}

model CompanyDocument {
  id             String    @id @db.VarChar(128)
  tenantId       String    @map("tenant_id") @db.VarChar(128)
  name           String
  url            String
  uploadDate     DateTime  @default(now()) @map("upload_date") @db.Timestamptz(6)
  expirationDate DateTime? @map("expiration_date") @db.Timestamptz(6)
  docType        String    @default("file") @map("doc_type")
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt      DateTime  @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant         Tenant    @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("company_documents")
}

model ExternalOrganization {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("external_organizations")
}

model ErpState {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  category  String
  data      Json     @default("[]")
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("erp_state")
}

model WorkpackRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("workpacks")
}

model MaintenanceTaskCardRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("maintenance_task_cards")
}



model ToolRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("tools")
}

model ActiveFlightSessionRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("active_flight_sessions")
}

model AlertRecord {
  id        String   @id @db.VarChar(128)
  tenantId  String   @map("tenant_id") @db.VarChar(128)
  data      Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Cascade)

  @@map("alerts")
}




